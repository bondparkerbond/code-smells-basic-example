<!-- <%= form_for(@cat) d o f %>
  <% i f @cat.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cat.errors.count, error) %> prohibited this cat from being saved:</h2>

      <ul>
      <% @cat.errors.full_messages.each d o message %>
        <li><%= message %></li>
      <% e nd %>
      </ul>
    </div>
  <% e nd %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% e nd %> -->
